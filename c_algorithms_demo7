 #include <stdio.h>
 #include <math.h>
char a[]="hello world";
int indexof(char letter)
{
	int i=0;
	while(a[i]!='\0')
	{
	  if(a[i]==letter)
	    return i;
	  i++;		
	}
	return -1;
}
 int main(){
 	printf("%d   %d\n",indexof('a'),indexof('w'));
    return 0;	
 }
